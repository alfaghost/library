<!-- Page Content -->
<div class="container">
  <div class="row">
    <div class="col-md-3">
      <p class="lead">Categories</p>
      <%= link_to 'All', root_path, class: 'list-group-item' %>
      <%= render partial: 'shared/category', collection: @categories %>
    </div>

    <div class="col-md-9">
      <p class="lead"><%= @category? @category.name : 'All' %></p>
      <div class="row">
        <%= render partial: 'shared/book', collection: @books %>
      </div>
    </div>
  </div>
</div>
<!-- /.container -->
